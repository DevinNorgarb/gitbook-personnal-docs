import{_ as t,o as i,c as o,ag as s}from"./chunks/framework.DXGyWiRo.js";const m=JSON.parse('{"title":"Grow Filesystem while running","description":"Credit https://askubuntu.com/questions/116351/increase-partition-size-on-which-ubuntu-is-installed","frontmatter":{"description":"Credit https://askubuntu.com/questions/116351/increase-partition-size-on-which-ubuntu-is-installed"},"headers":[],"relativePath":"software-engineering/linux/grow-filesystem-while-running.md","filePath":"software-engineering/linux/grow-filesystem-while-running.md"}'),a={name:"software-engineering/linux/grow-filesystem-while-running.md"};function n(r,e,l,d,h,c){return i(),o("div",null,[...e[0]||(e[0]=[s('<h1 id="grow-filesystem-while-running" tabindex="-1">Grow Filesystem while running <a class="header-anchor" href="#grow-filesystem-while-running" aria-label="Permalink to &quot;Grow Filesystem while running&quot;">​</a></h1><p>As a matter of fact, you <strong>CAN</strong> <em>enlarge</em> the root filesystem while Ubuntu is running (I learned this recently myself <a href="https://askubuntu.com/a/109224/14564" target="_blank" rel="noreferrer">here</a>) - this sounds incredible but it&#39;s true 😃</p><p>Here&#39;s the list of steps for a simple scenario where you have two partitions, <code>/dev/sda1</code> is an ext4 partition the OS is booted from and <code>/dev/sdb2</code> is swap. For this exercise we want to remove the swap partition an extend <code>/dev/sda1</code> to the whole disk.</p><ol><li>As always, make sure you have a backup of your data - since we&#39;re going to modify the partition table there&#39;s a chance to lose all your data if you make a typo, for example.</li><li>Run <code>sudo fdisk /dev/sda</code><ul><li>use <code>p</code> to list the partitions. Make note of the start cylinder of <code>/dev/sda1</code></li><li>use <code>d</code> to <strong>delete</strong> first the swap partition (<code>2</code>) and then the <code>/dev/sda1</code> partition. This is very scary but is actually harmless as the data is not written to the disk until you write the changes to the disk.</li><li>use <code>n</code> to <strong>create</strong> a new primary partition. Make sure its start cylinder is exactly the same as the old <code>/dev/sda1</code> used to have. For the end cylinder agree with the default choice, which is to make the partition to span the whole disk.</li><li>use <code>a</code> to toggle the bootable flag on the new <code>/dev/sda1</code></li><li>review your changes, take a deep breath and use <code>w</code> to write the new partition table to disk. You&#39;ll get a message telling that the kernel couldn&#39;t re-read the partition table because the device is busy, but that&#39;s ok.</li></ul></li><li>Reboot with <code>sudo reboot</code>. When the system boots, you&#39;ll have a smaller <em>filesystem</em> living inside a larger <em>partition</em>.</li><li>The next magic command is <code>resize2fs</code>. Run <code>sudo resize2fs /dev/sda1</code> - this form will default to making the filesystem to take all available space on the partition.</li></ol><p>That&#39;s it, we&#39;ve just resized a partition on which Ubuntu is installed, without booting from an external drive.</p>',5)])])}const p=t(a,[["render",n]]);export{m as __pageData,p as default};
